# TravelBird design challenge

To use this:

```shell
$ git clone https://github.com/chielkunkels/travelbird.git
$ cd travelbird
$ npm install
$ node
```

Then it should be running on port 5001, so you can access it at
[http://localhost:5001/](http://localhost:5001/).

